export enum enmRole {
  admin = "admin",
  jobseeker = "jobseeker",
}

export enum enmApplicationStatus {
  submitted = "submitted",
  reviewed = "reviewed",
  rejected = "rejected",
}

export enum enmJobStatus {
  open = "open",
  closed = "closed",
}
